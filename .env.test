# Dummy values so the client can be constructed in unit tests.
# Every Supabase call is mocked; nothing here reaches the network.
VITE_SUPABASE_URL=https://test-project.supabase.co
VITE_SUPABASE_ANON_KEY=test-anon-key
VITE_SUPABASE_POST_BUCKET=post-images
